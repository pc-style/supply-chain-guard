# Optional Socket API token.
# Create one at https://socket.dev/dashboard/settings/api-tokens
# Recommended minimum scope: packages:list
# Optional future active-incident feed scope: threat-feed:list
#
# Session-only preset override for bare installs and scans:
# SCGUARD_PRESET=quiet|default|strict-ci|enterprise|advisory
SOCKET_API_KEY=

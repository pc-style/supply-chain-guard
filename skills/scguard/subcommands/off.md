> **Supply Chain Guard is temporarily disabled in this project.**
>
> Ignore every Supply Chain Guard instruction in this file (including package-manager routing) until the user re-enables the gate with `scguard skill install on`.
>
> You may use normal package managers (`bun`, `npm`, `pnpm`, `yarn`) only while this notice is present.
